# Быстрая инструкция по деплою на DigitalOcean

## 1. Отправка в GitHub

```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/grinevoleg/winplacearena.git
git branch -M main
git push -u origin main
```

## 2. Создание приложения в DigitalOcean

1. Зайдите на https://cloud.digitalocean.com/apps
2. Нажмите **"Create App"**
3. Выберите **GitHub** → авторизуйтесь
4. Выберите репозиторий `grinevoleg/winplacearena`
5. Выберите ветку `main`
6. DigitalOcean автоматически обнаружит `.do/app.yaml`

## 3. Настройка переменных окружения

В настройках Backend сервиса добавьте:
- `OPENAI_API_KEY` (тип: SECRET) - ваш OpenAI ключ (опционально)

## 4. Добавление базы данных

1. В настройках приложения нажмите **"Add Database"**
2. Выберите **PostgreSQL**
3. Выберите план (можно самый дешевый для начала)
4. `DATABASE_URL` добавится автоматически

## 5. Деплой

1. Нажмите **"Create Resources"**
2. Дождитесь завершения (5-10 минут)
3. После деплоя получите URL приложения

## 6. Настройка FRONTEND_URL

**Где найти URL:**
1. В панели DigitalOcean Apps откройте ваше приложение
2. В разделе **"Components"** найдите:
   - **Frontend URL**: скопируйте этот URL
   - **Backend URL**: проверьте, что он работает

**Настройка:**
1. Зайдите в настройки **Backend** сервиса
2. Раздел **"Environment Variables"**
3. Добавьте переменную `FRONTEND_URL` = URL вашего фронтенда (скопируйте из карточки frontend)
4. Сохраните и перезапустите

## Готово! 🚀

Ваше приложение доступно по адресу фронтенда.

**Примечание:** После каждого push в `main` приложение автоматически передеплоится.

